{
  "name": "fiter-blog",
  "private": true,
  "scripts": {
    "build:blog": "node scripts/build-blog.js",
    "build:assets": "node scripts/build.js",
    "build:sitemap": "node scripts/build-sitemap.js",
    "build:tailwind": "tailwindcss -c tailwind.config.js -o assets/css/tailwind.min.css --minify",
    "build": "npm run build:tailwind && npm run build:assets && npm run build:blog && npm run build:sitemap"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.2",
    "csso": "^5.0.5",
    "sharp": "^0.34.5",
    "tailwindcss": "^4.3.2",
    "terser": "^5.48.0"
  }
}
